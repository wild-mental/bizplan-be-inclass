# Gradle 데몬 프로세스의 메모리 사용량을 제한합니다 (512MB)
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m
# 메모리 낭비를 막기 위해 데몬을 사용하지 않습니다 (중요)
org.gradle.daemon=false
# 병렬 빌드를 끕니다 (메모리 절약)
org.gradle.parallel=false

